# Sanity CMS — copy this file to `.env.local` and fill in the values.
# Both are NEXT_PUBLIC_ (exposed to the browser), so they are not secrets.
# The same values are configured in the Vercel project settings.

NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=production
